/* ── Knowledge Bank API Routes — stub ──
   Server-side KB operations. Wired in Phase 3.
   KB persistence is primarily client-side (IndexedDB),
   but these routes provide optional server-side query/export. */

export function knowledgeBankRoutes(router) {
  // Stub — implemented in Phase 3
  return router;
}
