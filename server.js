/**
 * Redirector — use the new server entry point.
 * Run:  node server/index.js   (or:  npm run server)
 */
console.log('\n  Server has moved. Run:\n');
console.log('    node server/index.js');
console.log('    — or —');
console.log('    npm run server\n');
process.exit(0);
